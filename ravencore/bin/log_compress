#!/bin/bash

. /etc/ravencore.conf || exit 1

compress=$1
domain_name=$2
log_file=$3

case $compress in
    "--compress") /bin/gzip -9 $VHOST_ROOT/$domain_name/logs/$log_file;;
    "--decompress") /bin/gunzip $VHOST_ROOT/$domain_name/logs/$log_file;;
esac
