#!/bin/bash

. /etc/ravencore.conf || exit 1

domain_name=$1
mail_name=$2

if [ -z "$domain_name" ] || [ -z "$mail_name" ]; then

    echo "You must supply a domain name and mail name"

    exit 1

fi

rm -rf $VMAIL_ROOT/$domain_name/$mail_name
